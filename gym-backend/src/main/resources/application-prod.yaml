server:
  port: ${PORT:9999}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false
  sql:
    init:
      mode: always

cors:
  allowed-origins: ${CORS_ORIGINS}
